#pragma once

#include <string>

namespace Constants {
    inline constexpr int SCREEN_WIDTH = 1300;
    inline constexpr int SCREEN_HEIGHT = 800;
    inline constexpr int FRAMERATE_LIMIT = 60;
    inline constexpr const char* WINDOW_NAME = "Pong";
}  